#################################################################
#####  2. Min
#################################################################

# Function to calculate minimum value in each row
scalogram.calculate_min <- function(row) {
  min_val <- min(row)
  return(min_val)
}
