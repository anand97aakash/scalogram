################################################################
#####  3. Max
################################################################

# Function to calculate maximum value in each row
scalogram.calculate_max <- function(row) {
  max_val <- max(row)
  return(max_val)
}
